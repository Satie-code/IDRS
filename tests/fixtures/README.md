# Test Fixtures

Shared, small, hand-crafted fixture files for tests (not real sensor data —
see `data/README.md` for why real datasets never live under `tests/`).

Empty in Phase 0; populated as later phases need fixture files.
