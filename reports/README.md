# Reports

Generated reports (coverage HTML, benchmark output, experiment summaries)
land here and are gitignored by default (`reports/*` in `.gitignore`) since
they're regenerable. This README is tracked so the directory survives a
clone; individual reports are not.
