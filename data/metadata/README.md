# Dataset Metadata

Small, hand-written documentation about datasets: schemas, sensor channel
definitions, train/val/test split manifests, provenance notes. See
`data/README.md` for why this is the one `data/` subfolder tracked in Git.

Empty in Phase 0.
