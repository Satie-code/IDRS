# Benchmarks

Reproducible performance/accuracy benchmarks (Rule 7 in
`docs/development/engineering_rules.md`): pinned inputs, recorded
environment, checked-in benchmark code. Empty in Phase 0 — nothing to
benchmark until navigation code exists.
